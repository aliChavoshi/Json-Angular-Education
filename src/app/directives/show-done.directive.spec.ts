import { ShowDoneDirective } from './show-done.directive';

describe('ShowDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDoneDirective();
    expect(directive).toBeTruthy();
  });
});
